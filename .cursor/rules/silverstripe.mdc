---
description: Talaria Silverstripe module conventions
globs:
  - "silverstripe/**"
  - "_config/**"
  - "_config.php"
---

# Silverstripe adapter

Depends on core `Talaria\Client`. Integrate via Injector + Monolog — minimal app code changes.

- `TalariaLogHandler` extends Monolog `AbstractProcessingHandler`
- Config via YAML / env (`TALARIA_DSN`, `TALARIA_API_KEY`, …)
- Capture Member id and request context when available
- Do not put Silverstripe imports in `src/`
- Same batch queue semantics as core SDK
